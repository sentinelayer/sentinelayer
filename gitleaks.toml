title = "SentinelLayer Gitleaks Config"
[default]
severity = "medium"
description = "SentinelLayer secret scanning per blueprint 2026"
[rules]
[[rules]]
id = "sentinel-layer-api-key"
description = "SentinelLayer API key pattern"
regex = "(sentinellayer|sl_|sentinel_)[a-zA-Z0-9_]{20,}"
tags = ["credential"]
