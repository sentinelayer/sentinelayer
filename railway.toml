[build]
builder = "NIXPACKS"
buildCommand = "pip install -e . && python scripts/run_migrations.py"

[deploy]
startCommand = "uvicorn src.sentinelayer.api.main_full:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
