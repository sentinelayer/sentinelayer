version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: sentinelayer-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - sentinelayer-network

volumes:
  redis_data:

networks:
  sentinelayer-network:
    driver: bridge
